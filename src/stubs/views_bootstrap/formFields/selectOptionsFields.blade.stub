<div class="mb-3">
    <label class="form-label">{{FIELD_LABEL}}</label>
    <select
        name="{{FIELD_NAME}}"
        class="form-select @error('{{FIELD_NAME}}') is-invalid @enderror">
        <option value="">Select</option>
{{OPTIONS}}
    </select>

    @error('{{FIELD_NAME}}')
        <div class="invalid-feedback">{{ $message }}</div>
    @enderror
</div>
